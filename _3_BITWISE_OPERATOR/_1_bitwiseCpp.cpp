#include <iostream>
using namespace std;
int main()
{
    int x = 3;
    int y = 6;
    int z = 33;

    // AND OPERATOR
    // cout << (x & y) << endl;

    // OR OPERATOR
    // cout << (x | y) << endl;

    // XOR OPERATOR
    // cout << (x ^ y) << endl;

    // LEFT SHIFT OPERATOR
    // cout << (x << 1) << endl;
    // cout << (x << 2) << endl;
    // cout << (x << 3) << endl;
    // cout << (x << y) << endl;
    // x << y = x * (2 ^ y)

    // RIGHT SHIFT OPERATOR
    // cout << (z >> 1) << endl;
    // cout << (z >> 2) << endl;
    // cout << (z >> 3) << endl;
    // cout << (z >> 4) << endl;
    // cout << (z >> y) << endl;
    // x >> y = floor(x / (2 ^ y))

    // NOT OPERATOR
    // -> UNSIGNED
    // unsigned int a = 1;
    // cout << (~a) << endl;
    // unsigned int b = 5;
    // cout << (~b) << endl;
    // -> SIGNED
    // cout << (~x) << endl;
    // 2's compliment => 2^n - x
    // 2^32 - 1 - x;
}